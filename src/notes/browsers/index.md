# Browsers

Type clipboard into browser: `type_clipboard.js`

## clear browser cache

### browser DNS cache

Chrome:

```url
chrome://net-internals/#dns
```

Firefox:

```url
about:networking#dns
```
