# Agent instructions

(authored by human unless marked 🤖)

Put notes into relevant sections of files. Use Minimal Bullet Tree.

Every documentation file MUST state its default authorship directly below its title:

- `(authored by agents unless marked 🧑)`
- `(authored by human unless marked 🤖)`

Put the opposite emoji on the heading or paragraph containing each exception;
it applies only to that adjacent content. `AGENTS.md` files are human-authored
unless the human explicitly says otherwise. If the authorship of an existing
file is unknown, ask the human; never guess.
